import { FlyHerosImpurePipe } from './fly-heros-impure.pipe';

describe('FlyHerosImpurePipe', () => {
  it('create an instance', () => {
    const pipe = new FlyHerosImpurePipe();
    expect(pipe).toBeTruthy();
  });
});
