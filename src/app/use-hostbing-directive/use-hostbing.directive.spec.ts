import { UseHostbingDirective } from './use-hostbing.directive';

describe('UseHostbingDirective', () => {
  it('should create an instance', () => {
    const directive = new UseHostbingDirective();
    expect(directive).toBeTruthy();
  });
});
